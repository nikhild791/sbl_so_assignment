name: Deploy the api
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./docker/Dockerfile.api
          push: true
          tags: nikhil791/sbl_api:${{ github.sha }}
      - name: Deploy to the VM
        env:
          SHA_TAG: ${{ github.sha }}
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ssh_key
          chmod 600 ssh_key

          ssh -i ssh_key -o StrictHostKeyChecking=no sbl@34.122.35.40 << EOF
            cd /home/sbl/sbl_so

            # Ensure .env exists
            touch .env

            # Write IMAGE_TAG correctly
            sed -i "s/^IMAGE_TAG=.*/IMAGE_TAG=$SHA_TAG/" .env || echo "IMAGE_TAG=$SHA_TAG" >> .env

            docker compose pull
            docker compose up -d
          EOF
